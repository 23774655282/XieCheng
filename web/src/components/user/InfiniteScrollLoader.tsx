/**
 * 用户端 - 上滑自动加载（IntersectionObserver 或滚动 + 分页）
 */
export default function InfiniteScrollLoader() {
  return (
    <div className="infinite-scroll-loader" style={{ padding: '1rem', textAlign: 'center', color: '#999' }}>
      [InfiniteScrollLoader]
    </div>
  );
}
